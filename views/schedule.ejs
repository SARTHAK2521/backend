<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FitTrack Pro | Schedule</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #00a8ff;
            --primary-dark: #0097e6;
            --accent: #feca57;
            --dark: #1e272e;
            --light: #f5f6fa;
            --gray: #dcdde1;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #121212;
            color: var(--light);
            line-height: 1.6;
        }
        
        /* Header Styling */
        header {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: #fff;
            padding: 1.5rem 2rem;
            box-shadow: 0 4px 20px rgba(0, 168, 255, 0.3);
        }
        
        .header-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }
        
        .logo i {
            font-size: 2rem;
            color: var(--accent);
        }
        
        .logo-text h1 {
            margin: 0;
            font-size: 1.8rem;
            font-weight: 700;
        }
        
        .logo-text p {
            font-size: 0.9rem;
            margin: 0;
            opacity: 0.9;
        }
        
        .nav-links {
            display: flex;
            gap: 1.5rem;
            align-items: center;
        }
        
        .nav-links a {
            color: #fff;
            text-decoration: none;
            font-weight: 500;
            font-size: 1rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .nav-links a:hover {
            color: var(--accent);
        }
        
        .profile-link {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            background-color: rgba(255, 255, 255, 0.1);
            padding: 0.5rem 1rem;
            border-radius: 50px;
            color: white;
            text-decoration: none;
            transition: all 0.3s ease;
        }
        
        .profile-link:hover {
            background-color: rgba(255, 255, 255, 0.2);
        }
        
        /* Main Content */
        main {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 0 2rem;
        }
        
        .welcome-section {
            text-align: center;
            margin-bottom: 3rem;
            animation: fadeIn 1s ease;
        }
        
        .welcome-section h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--light);
        }
        
        .welcome-section .username {
            color: var(--accent);
            font-weight: 600;
        }
        
        .welcome-section p {
            font-size: 1.1rem;
            color: var(--gray);
            max-width: 700px;
            margin: 0 auto;
        }
        
        /* Calendar Styling */
        .calendar {
            background: rgba(30, 39, 46, 0.8);
            border-radius: 15px;
            padding: 2rem;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .calendar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }
        
        .calendar-header h2 {
            font-size: 1.8rem;
            color: var(--primary);
        }
        
        .days-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
        }
        
        .day-card {
            background: rgba(44, 62, 80, 0.6);
            border-radius: 12px;
            padding: 1.5rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
        
        .day-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0, 168, 255, 0.1);
        }
        
        .day-card h3 {
            color: var(--primary);
            margin-bottom: 1rem;
            font-size: 1.3rem;
            border-bottom: 2px solid rgba(0, 168, 255, 0.3);
            padding-bottom: 0.5rem;
        }
        
        .workouts-list {
            min-height: 200px;
        }
        
        .workout-item {
            background: rgba(0, 168, 255, 0.1);
            border-left: 3px solid var(--primary);
            padding: 1rem;
            margin-bottom: 1rem;
            border-radius: 0 8px 8px 0;
            transition: all 0.3s ease;
        }
        
        .workout-item:hover {
            background: rgba(0, 168, 255, 0.2);
        }
        
        .workout-item h4 {
            color: var(--light);
            margin-bottom: 0.5rem;
            font-size: 1.1rem;
        }
        
        .workout-item p {
            color: var(--gray);
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }
        
        .workout-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: var(--gray);
            font-size: 0.85rem;
        }
        
        .delete-workout {
            background: none;
            border: none;
            color: #ff6b6b;
            cursor: pointer;
            transition: color 0.3s ease;
        }
        
        .delete-workout:hover {
            color: #ff5252;
        }
        
        .add-workout {
            margin-top: 1rem;
        }
        
        .add-workout button {
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.8rem 1.5rem;
            border-radius: 50px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin: 0 auto;
        }
        
        .add-workout button:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 168, 255, 0.3);
        }
        
        /* Modal Styles */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            z-index: 1000;
            backdrop-filter: blur(5px);
        }
        
        .modal-content {
            background: rgba(44, 62, 80, 0.95);
            margin: 10% auto;
            padding: 2rem;
            border-radius: 15px;
            width: 90%;
            max-width: 500px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
        }
        
        .close-modal {
            position: absolute;
            top: 1rem;
            right: 1.5rem;
            font-size: 1.5rem;
            color: var(--gray);
            cursor: pointer;
            transition: color 0.3s ease;
        }
        
        .close-modal:hover {
            color: var(--light);
        }
        
        .modal-content h2 {
            color: var(--primary);
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
        }
        
        .form-group {
            margin-bottom: 1.5rem;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--light);
            font-weight: 500;
        }
        
        .form-group select,
        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 0.8rem 1rem;
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            color: var(--light);
            font-family: 'Poppins', sans-serif;
        }
        
        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }
        
        .btn-primary {
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.8rem 1.5rem;
            border-radius: 50px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            width: 100%;
        }
        
        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 168, 255, 0.3);
        }
        
        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
            }
            
            .nav-links {
                width: 100%;
                justify-content: center;
                flex-wrap: wrap;
            }
            
            .welcome-section h1 {
                font-size: 2rem;
            }
            
            .days-grid {
                grid-template-columns: 1fr;
            }
        }
        footer {
            background: var(--dark);
            color: var(--gray);
            padding: 2rem;
            text-align: center;
            margin-top: 3rem;
        }
        
        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        
        .footer-logo {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            margin-bottom: 1rem;
        }
        
        .footer-logo i {
            font-size: 1.5rem;
            color: var(--primary);
        }
        
        .footer-logo span {
            font-size: 1.2rem;
            font-weight: 700;
        }
        
        .footer-links {
            display: flex;
            gap: 1.5rem;
            margin-bottom: 1.5rem;
            flex-wrap: wrap;
            justify-content: center;
        }
        
        .footer-links a {
            color: var(--gray);
            text-decoration: none;
            transition: color 0.3s ease;
        }
        
        .footer-links a:hover {
            color: var(--primary);
        }
        
        .social-links {
            display: flex;
            gap: 1.5rem;
            margin-bottom: 1.5rem;
        }
        
        .social-links a {
            color: var(--gray);
            font-size: 1.2rem;
            transition: color 0.3s ease;
        }
        
        .social-links a:hover {
            color: var(--primary);
        }
        
        .copyright {
            font-size: 0.9rem;
            opacity: 0.7;
        }
        
    </style>
</head>
<body>
    <header>
        <div class="header-content">
            <div class="logo">
                <i class="fas fa-dumbbell"></i>
                <div class="logo-text">
                    <h1>FitTrack Pro</h1>
                    <p>Your personal fitness companion</p>
                </div>
            </div>
            <nav class="nav-links">
                <a href="/"><i class="fas fa-home"></i> Home</a>
                <a href="/schedule"><i class="fas fa-calendar-alt"></i> Schedule</a>
                <a href="/diet"><i class="fas fa-utensils"></i> Nutrition</a>
                <a href="/bmi" class="profile-link">
                    <i class="fas fa-user"></i>
                    <span>BMI</span>
                </a>
            </nav>
        </div>
    </header>

    <main>
        <section class="welcome-section">
            <h1>Welcome back, <span class="username"><%= username %></span></h1>
            <p>Track your progress, stay motivated, and achieve your fitness goals with our comprehensive tools and analytics.</p>
        </section>

        <section class="calendar">
            <div class="calendar-header">
                <h2>This Week's Workout Plan</h2>
            </div>
            
            <div class="days-grid">
                <% const days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday']; %>
                <% days.forEach(day => { %>
                    <div class="day-card">
                        <h3><%= day %></h3>
                        <div class="workouts-list">
                            <% workouts.filter(w => w.day.toLowerCase() === day.toLowerCase()).forEach(workout => { %>
                                <div class="workout-item">
                                    <h4><%= workout.type.charAt(0).toUpperCase() + workout.type.slice(1) %></h4>
                                    <p><%= workout.notes %></p>
                                    <div class="workout-meta">
                                        <span><%= workout.duration %> minutes</span>
                                        <form action="/schedule/delete/<%= workout.id %>" method="POST" style="display: inline;">
                                            <button type="submit" class="delete-workout">
                                                <i class="fas fa-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </div>
                            <% }); %>
                            <div class="add-workout">
                                <button class="btn btn-primary" onclick="openAddWorkoutModal('<%= day %>')">
                                    <i class="fas fa-plus"></i> Add Workout
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        </section>
    </main>

    <!-- Add Workout Modal -->
    <div class="modal" id="addWorkoutModal">
        <div class="modal-content">
            <span class="close-modal" onclick="closeModal()">&times;</span>
            <h2>Add Workout for <span id="modal-day"></span></h2>
            <form id="workout-form" action="/schedule/add" method="POST">
                <input type="hidden" name="day" id="workout-day">
                <div class="form-group">
                    <label for="workout-type">Workout Type</label>
                    <select name="type" id="workout-type" required>
                        <option value="">Select type</option>
                        <option value="cardio">Cardio</option>
                        <option value="strength">Strength</option>
                        <option value="flexibility">Flexibility</option>
                        <option value="hiit">HIIT</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="workout-duration">Duration (minutes)</label>
                    <input type="number" name="duration" id="workout-duration" min="5" max="180" required>
                </div>
                <div class="form-group">
                    <label for="workout-notes">Notes</label>
                    <textarea name="notes" id="workout-notes" rows="3"></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Save Workout</button>
            </form>
        </div>
    </div>
    <footer>
        <div class="footer-content">
            <div class="footer-logo">
                <i class="fas fa-dumbbell"></i>
                <span>FitTrack Pro</span>
            </div>
            <div class="footer-links">
                <a href="#">About Us</a>
                <a href="#">Features</a>
                <a href="#">Pricing</a>
                <a href="#">Blog</a>
                <a href="#">Contact</a>
                <a href="#">Privacy Policy</a>
            </div>
            <div class="social-links">
                <a href="#"><i class="fab fa-facebook-f"></i></a>
                <a href="#"><i class="fab fa-twitter"></i></a>
                <a href="https://www.instagram.com/sarthakjoshi.14/"><i class="fab fa-instagram"></i></a>
                <a href="#"><i class="fab fa-youtube"></i></a>
                <a href="#"><i class="fab fa-linkedin-in"></i></a>
            </div>
            <div class="copyright">
                &copy; 2025 FitTrack Pro. All rights reserved.
            </div>
        </div>
    </footer>

    <script>
        // Modal functions
        function openAddWorkoutModal(day) {
            document.getElementById('modal-day').textContent = day;
            document.getElementById('workout-day').value = day;
            document.getElementById('addWorkoutModal').style.display = 'block';
        }

        function closeModal() {
            document.getElementById('addWorkoutModal').style.display = 'none';
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            const modal = document.getElementById('addWorkoutModal');
            if (event.target === modal) {
                closeModal();
            }
        }
    </script>
</body>
</html>